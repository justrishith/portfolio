/**
 * Static articles content.
 *
 * Articles are not used in this deployment — the array is kept empty
 * so feature internals still type-check. Add entries here to bring
 * blogging back, then re-add the `/articles` routes from upstream
 * next-folio history.
 */

import type { Article } from "@/features/articles";

export const articlesData: readonly Article[] = [];
